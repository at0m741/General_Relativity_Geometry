#include "include.h"


/*  */
/* void init_grid() { */
/*     for (int i = 0; i < NR; i++) { */
/*         r_grid[i] = r_min + (r_max - r_min) * i / (NR - 1);  */
/* 		printf("r_grid[%d] = %f\n", i, r_grid[i]); */
/* 	} */
/*     for (int i = 0; i < NTHETA; i++) { */
/*         theta_grid[i] = theta_min + (theta_max - theta_min) * i / (NTHETA - 1); */
/* 		printf("theta_grid[%d] = %f\n", i, theta_grid[i]); */
/*     } */
/*     for (int i = 0; i < NPHI; i++) { */
/*         phi_grid[i] = phi_min + (phi_max - phi_min) * i / (NPHI - 1); */
/* 		printf("phi_grid[%d] = %f\n", i, phi_grid[i]); */
/*     } */
/* } */
/*  */
/*  */
/*  */
/*  */
/*  */
